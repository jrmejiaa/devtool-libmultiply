# Multiply library as example for devtool

This repository is a very basic static library that is used on my tutorial about devtool about **How to use devtool to create and test a custom library**. In order to build it locally, you need to have available in your system the GoogleTest library. Otherwise the system will failed by not finding the `pkgconfig` file.
